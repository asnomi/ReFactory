# ブランチ戦略

## ポートフォリオサイト（asnoni.com）リプレイス

**対象リポジトリ:** [github.com/asnomi/ReFactory](https://github.com/asnomi/ReFactory)
**関連:** [task.md](./task.md)（WBS・進捗管理）

---

## 1. ブランチ構成

3階層構成とする。

| ブランチ | 役割 | 直接コミット | マージ元 |
| --- | --- | --- | --- |
| `master` | 本番相当（リリース済み・常にデプロイ可能な状態を保つ） | 禁止 | `dev` からのみ |
| `dev` | 開発統合ブランチ（各トラックの成果物を集約） | 禁止（featureからのPRのみ） | `feature/*` からのPRのみ |
| `feature/*` | 各トラックの作業ブランチ | 可 | `dev` から作成 |

```
master ← (フェーズゲート/リリース時にdevをマージ)
  ↑
 dev   ← (PRマージ)
  ↑
feature/xxx  ← (各トラックが作業)
```

---

## 2. 各ブランチの詳細

### 2.1 `master`

- 常にデプロイ可能（S3+CloudFrontへのデプロイ対象）な状態を保つ。
- `dev` からのマージのみで更新する。直接コミット・直接PRは行わない。
- マージのタイミングは [task.md](./task.md) のフェーズゲート（例: Phase 2完了時、Phase 3完了時等）に合わせる。マージ判断はPJ管理／レビューセッションが行う。

### 2.2 `dev`

- 各トラックの成果物を集約する統合ブランチ。現時点の最新設計・実装が常にここに揃う。
- `feature/*` からのPRをマージすることで更新する。
- **PJ管理／レビューセッションがPRのレビュー・マージを担当**する（従来通りフェーズゲートのタイミングで声がけする運用を継続）。

### 2.3 `feature/*`

- 各トラック（A/B/C）がタスク単位で作業するブランチ。
- `dev` から都度分岐して作成する。
- 作業中はこのブランチへ自由にcommitしてよい。
- タスクが完了したら `dev` 宛にPRを作成する。

---

## 3. ブランチ命名規則

```
feature/<トラック識別子>-<内容を表す短い英語スラッグ>
```

| トラック識別子 | 対応セッション | 例 |
| --- | --- | --- |
| `track-a` | フロントエンド（Kross） | `feature/track-a-hero-override` |
| `track-b` | Notionデータ連携 | `feature/track-b-notion-sync-script` |
| `track-c` | インフラ／CI-CD | `feature/track-c-github-actions-workflow` |

- 1つのタスク（task.mdのTxxx単位を目安）につき1ブランチを基本とする。タスクが大きい場合は分割してよい。
- 英語スラッグは内容が分かる範囲で簡潔にする（日本語不可、ハイフン区切り）。

---

## 4. ワークフロー

1. 各トラックのセッションで、着手するタスクに対応する `feature/*` ブランチを最新の `dev` から作成する。
   ```bash
   git checkout dev
   git pull origin dev
   git checkout -b feature/track-a-xxx
   ```
2. 作業しながら `feature/*` ブランチへ随時commitする。
3. タスクが完了したら、リモートへpushし `dev` 宛にPRを作成する。
   ```bash
   git push -u origin feature/track-a-xxx
   gh pr create --base dev --head feature/track-a-xxx
   ```
4. PJ管理／レビューセッションがPR内容をレビューする（要件定義書・task.mdとの整合性、他トラックとの不整合の有無を確認）。
5. 問題なければ `dev` へマージ。マージ後、不要になった `feature/*` ブランチは削除する。
6. フェーズゲートのタイミングで、`dev` の内容を `master` へマージする（PJ管理／レビューセッションが実施）。

---

## 5. マージ方式・コミットメッセージ

- `feature → dev` : Squash mergeを基本とする（feature内の細かいcommit履歴はPR単位にまとめ、devの履歴を追いやすくする）。
- `dev → master` : 通常のマージ（履歴を保持し、どのdev時点がリリースされたか追跡できるようにする）。
- コミットメッセージは日本語で可。1行目に変更内容の要約、必要に応じて本文で背景・理由を補足する（このリポジトリのこれまでのcommitスタイルに準拠）。

---

## 6. 補足

- 現時点（2026-08-25）では `master`/`dev` の分岐・ブランチ保護ルールはGitHub側で未設定。次回のフェーズゲート前までにリポジトリ側でも以下を設定することを推奨する（実施はリポジトリ管理者側で対応）。
  - `master`・`dev` への直接push禁止（PR経由のみ許可）
  - `dev` へのマージ前にPRレビュー必須化
- トラック間の依存・不整合（例: content/ディレクトリ構成、themes/kross運用方針など）は [task.md](./task.md) の該当タスクに記載の通り、featureブランチでの作業中に解消し、PR時点で解消済みであることが望ましい。未解消のままdevへマージする場合はPR説明欄にその旨を明記すること。
