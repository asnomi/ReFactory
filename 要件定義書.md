# 要件定義書

# ポートフォリオサイト（asnoni.com）リプレイス 要件定義書

**Document Version:** 1.0

**Author:** 創作活動アシスタンス（マネージャー：もこ）

**Date:** 2026-07-15

## 1. プロジェクト概要

### 1.1 目的

現在のポートフォリオサイト（asnoni.com）の持つ洗練されたデザイン性と、AWS S3 + CloudFrontによる低コスト・高可用性なインフラ構成を維持しつつ、コンテンツ運用負担の軽減を目指す。

静的サイトジェネレーター「Hugo」と、日常的に作品管理を行っている「Notion」を連携させることで、執筆・作品追加からデプロイまでをシームレスに行う自動化パイプラインを確立する。

### 1.2 全体アーキテクチャ構成

- **CMS（マスターデータ）:** Notion Database
- **SSG（ビルドツール）:** Hugo（採用テーマ：Quint）
- **CI/CD（デプロイ自動化）:** GitHub Actions
- **ホスティング（インフラ）:** Amazon S3 + Amazon CloudFront

## 2. 機能要件 (Functional Requirements)

### 【Block 1：CMS・データ同期機能】

| **ID** | **機能名** | **要件内容** |
| --- | --- | --- |
| F-101 | **Notionデータ連携** | Notionの指定データベースから作品情報（タイトル、制作日、画像、説明文、タグ等）及びニュース記事をAPI経由で取得し、HugoのContent（Markdown）及びDataファイルに変換する。 |
| F-102 | **画像のローカル永続化** | Notion API経由の画像URL（1時間のアクセス有効期限あり）をそのまま使用せず、ビルド実行時にツール経由で画像をダウンロードし、Hugoの `assets/` 又は `static/` に保存してリンク切れを防止する。 |
| F-103 | **最新・新作フラグ自動化** | Notion側のメタデータ（公開日やステータス）を基に、Hugo側のビルド時に自動で条件判定を行い、新着記事や新作作品にフロントマター情報としてフラグ（`new: true` 等）を付与する。 |

### 【Block 2：サイトビルド・デプロイ自動化機能】

| **ID** | **機能名** | **要件内容** |
| --- | --- | --- |
| F-201 | **自動ビルド実行** | Notion側での更新トリガー（※）、またはリポジトリへの Push を検知し、GitHub Actions 上で Notion からのデータ同期スクリプトおよび `hugo` ビルドコマンドを実行する。 |
| F-202 | **S3同期・デプロイ** | ビルドで生成された静的ファイル（`public/` ディレクトリ配下）を Amazon S3 バケットへ自動同期（`aws s3 sync`）する。 |
| F-203 | **キャッシュ無効化** | S3への同期完了後、CloudFrontのキャッシュ無効化（Invalidation）を自動実行し、最新コンテンツを即時公開する。 |

*(※更新トリガーの具体的な発火条件は申し送り事項にて定義)*

### 【Block 3：画面・デザイン・コンテンツ表示機能】

| **ID** | **機能名** | **要件内容** |
| --- | --- | --- |
| F-301 | **テーマ「Quint」の適用** | Hugoのテーマ「Quint」をベースとし、現行サイトのアイデンティティを保つためのレイアウト及びスタイリング調整を行う。 |
| F-302 | **ヒーローエリア表示** | トップページ上部に、現行サイトと同様の大胆でインパクトのあるヒーロー画像及びコピーを配置する。 |
| F-303 | **ギャラリー（作品一覧）** | 過去作品をグリッド・カード状等で美しく閲覧できるギャラリーセクション（または専用ページ）を設置する。Notionのプロパティ（年、ジャンル等）による分類やソートを可能にする。 |
| F-304 | **ニュース・近況エリア** | 最新の活動情報を表示するニュースエリア（お知らせ・ブログ）を設置する。最新N件をトップページに露出させる。 |
| F-305 | **新作（NEW）視覚的バッジ** | F-103 で付与されたフラグを元に、ギャラリー及びニュース一覧の該当コンテンツに対して、視覚的に判別できる「NEW」バッジやアクセント表示を行う。 |

## 3. 非機能要件 (Non-Functional Requirements)

### 【Block 4：可用性・パフォーマンス】

| **ID** | **要件項目** | **要件内容** |
| --- | --- | --- |
| NF-101 | **可用性の維持** | サーバーレスである Amazon S3 ＋ CloudFront の構成により、突発的なアクセス増でもダウンしない高可用性を維持する。 |
| NF-102 | **画像処理・高速表示** | 写真・イラスト等の高解像度画像は、Hugoの Image Processing 機能等を用いてビルド時にWebPフォーマットへの変換や適切なリサイズを行い、ページのロード時間を最小限に抑える。 |

### 【Block 5：運用・保守性・コスト】

| **ID** | **要件項目** | **要件内容** |
| --- | --- | --- |
| NF-201 | **ゼロ・メンテナンス更新** | 日常の作品追加・お知らせ更新においては、ローカル環境のターミナルやコード編集を一切不要とし、「Notionアプリで記述・ステータス変更するだけ」で完結する運用を達成する。 |
| NF-202 | **インフラコストの最適化** | 動的サーバー（EC2やRDS等）や有料CMSサービスを用いないため、従量課金のみの月額数百円〜数千円レベルの低コスト運用を維持する。 |

## 4. 申し送り事項（要件不明瞭・今後の検討事項）

現時点の会話内容では明確化されていない、実装フェーズに向けて決定・検証すべき事項を以下にまとめる。

1. **NotionからGitHub Actionsへの「発火（トリガー）方式」の選定**
    - *課題:* Notionを更新した際、どうやってGitHub Actionsを動かすかの仕様決定が必要。
    - *候補案:*
        1. 定期実行（cron設定で1日に1〜2回決まった時間に自動ビルドする：設定が最楽でコストゼロ）。
        2. Notionオートメーション・Zapier・Make等の外部iPaaSを使って、Notionのプロパティ変更時（例：「公開」チェックボックスをONにした時）にGitHub Actionsの `repository_dispatch` webhookを叩く（即時更新が可能）。
2. **テーマ「Quint」のレイアウト・仕様に合わせたデータ構造のすり合わせ**
    - *課題:* 「Quint」の標準機能において、どこまでデフォルトのままギャラリー表示やニュース表示に適合できるか、ローカル環境でのフィジビリティテスト（PoC）が必要。
    - *対応:* ヒーローコンテンツの画像指定方法や、Quintが推奨する Markdown のフロントマター（ヘッダー情報）の構造に合わせた Notion → Hugo 変換ツールのマッピング定義を先行して行うこと。
3. **Notion同期ツールの技術選定**
    - *課題:* NotionからMarkdown/画像を生成するブリッジツールの選定。
    - *対応:* オープンソースの `notion-hugo` 等を利用するか、Notion APIと `notion-client` などを利用して小規模な独自スクリプト（Python / Node.js）をGitHub Actions内に組み込むかを、今後の実装ステップで検討・検証する。

## 5. 追加要件（Version 1.0以降の変更）

実装フェーズ中に追加された要件を記録する。既存ID体系との混同を避けるため、追加分は本セクションに追記する形とし、上記1〜4章の原文は変更しない。

| **ID** | **機能名** | **要件内容** | **追加日** |
| --- | --- | --- | --- |
| F-204 | **ブランチ別マルチ環境デプロイ** | `main`ブランチのビルド成果物は本番環境（`asnomi.com`）へ、`dev`ブランチのビルド成果物は検証環境（`dev.asnomi.com`）へ、それぞれ別々のデプロイ先（AWS CLIによるS3 push）に振り分けてデプロイする。 | 2026-08-26 |

**背景:** [ブランチ戦略.md](./ブランチ戦略.md)でmaster/dev/feature運用を定めたことに伴い、dev統合ブランチの成果物を本番に混在させず検証できる環境が必要になったため追加。実装はトラックC（インフラ/CI-CD）が担当。詳細な設計検討事項は[task.md](./task.md) T3-4参照。